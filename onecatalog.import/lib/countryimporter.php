<?php

namespace OneCatalog\Import;

/**
 * Слой CountryImporter (§3, §7): страна → свойство-список (дефолт) | HL.
 * find-or-create по label (§5.1). По умолчанию импорт страны выключен (§7).
 * TODO: адаптеры list/HL + назначение товару.
 */
final class CountryImporter
{
}
