<?php

namespace OneCatalog\Import;

/**
 * Слой BrandImporter (§3, §7): бренд → список (L, дефолт) | HL-справочник | раздел.
 * find-or-create по label (§5.1). Доп. поля бренда — через событие (сайтовый слой).
 * TODO: адаптеры list/HL/section + назначение товару.
 */
final class BrandImporter
{
}
